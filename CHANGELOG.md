## [0.2.0] - 2026-03-28

### Added
- Add integration test suite
- Add changelog crate and `update-changelog` justfile recipe

## [0.1.0] - 2026-03-28

### Added
- Add initial TUI interface for browsing syntax trees
- Add Go, Java, JavaScript, JSON, Rust, and TypeScript parsers
- Add `Python`, `C`, `C++`, `Ruby`, `Bash`, `HTML`, `CSS`, `TOML`, and `YAML` parsers
- Add Just grammar support
- Add interactive search with `/` to filter by node kind or text
- Add tree-sitter query support for all interaction modes
- Add yank-to-clipboard with flash notification on `y` keypress
- Add mouse support for click-to-navigate and scroll wheel
- Add `g`/`G` keybindings for jump-to-top/bottom navigation
- Add stdin support with `--language` flag
- Add non-interactive mode that prints the syntax tree by default
- Add tracebacks to error messages
- Add node coloring
- Add release workflow
